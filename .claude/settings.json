{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "STAGED=$(git diff --cached --name-only 2>/dev/null); if echo \"$STAGED\" | grep -q 'lib-claude-command/' && ! echo \"$STAGED\" | grep -q 'lib-claude-command/.mt-version'; then printf '{\"continue\":false,\"stopReason\":\"\\u26a0\\ufe0f lib-claude-command/ has changes but .mt-version was not bumped. Update lib-claude-command/.mt-version before committing.\"}'; else printf '{\"continue\":true}'; fi",
            "statusMessage": "Checking .mt-version bump..."
          }
        ]
      }
    ]
  }
}
